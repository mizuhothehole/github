Using Git by SSH

$ ssh-keygen -t rsa -C goldeyeqin@gmail.com ： generate ssh key into ～/.ssh

$ ssh-add -l ： see all local shh private keys
$ ssh-add： add private key in ～/.ssh into local port. make it contrainting ssh port.
$ ssh-add -d id_xxx.pub： delete a ssh key in ssh-agent
$ ssh-add -D：delete all ssh keys in ssh-agent

add ssh public key to github settings：https://github.com/settings/keys

$ ssh git@githun.com ：test ssh connection
$ git clone git@githun.com：userName/projectName.git ： download a github project and enable repo

under gerrit：
$ repo init ： this will make the repo tool installized in your local machine
$ repo sycn ： this will make the code downloaded into local. this is tried and the files will be
               downloaded to a folder named github
               Any way the data is downloaded from the port of ssh. A key is necessary
gitlist：by using this you can bind the data in local with the github site. Anyway I failed in
android studio on a Macbook
               
               

